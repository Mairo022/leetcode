package main

import (
	// "fmt"
	// "leetcode/all_disappeared_numbers"
	// "time"
)

func main() {
	// data := all_disappeared_numbers.Data
	// start := time.Now()

	// for _, s := range data {
	// 	fmt.Println(all_disappeared_numbers.FindDisappearedNumbers(s))
	// }

	// fmt.Println(time.Now().Sub(start))
}
